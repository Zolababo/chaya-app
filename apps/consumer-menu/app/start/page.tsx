import { ChayaRolePicker } from "@/components/chaya-role-picker";

/**
 * 손님·점주 역할 선택. 루트 `/` 는 기본적으로 `/t/demo` 로 바로 이동합니다.
 */
export default function StartRolePickerPage() {
  return <ChayaRolePicker />;
}
