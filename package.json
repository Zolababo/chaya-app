{
  "name": "chaya-platform",
  "private": true,
  "engines": {
    "node": ">=20.9.0"
  },
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "lint": "turbo lint",
    "verify": "turbo lint && turbo build"
  },
  "devDependencies": {
    "turbo": "^2.3.3"
  }
}
