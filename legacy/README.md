# 레거시 프로토타입 (참고용)

바이브 코딩 초기 단계의 **단일 HTML + CDN** 앱이다. 새 작업은 `apps/`·`packages/`에서 진행하고, 여기 파일은 **도메인 참고·스크린샷 대체** 용도로만 둔다.

| 폴더 | 내용 |
|------|------|
| `legacy/admin-app/` | 점주·관리자 프로토타입 HTML |
| `legacy/consumer-app/` | 고객 메뉴판 프로토타입 HTML |

실서비스·보안 기준에는 맞지 않으며, 필요 시 새 설계로 대체한다.
